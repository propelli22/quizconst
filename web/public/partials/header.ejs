<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page %> - Quizconst</title>
    <link rel="stylesheet" href="style/main.css">
</head>
<body>
    <header>
        <form method="get" id="language-form">
            <select name="language" id="language-selection" onchange="this.form.submit()">
                <option value="fi" <%= language === 'fi' ? 'selected' : '' %>>FI</option>
                <option value="en" <%= language === 'en' ? 'selected' : '' %>>EN</option>
            </select>
        </form>
        <h1 id="main-title">Quizconst</h1>
        <button id="nav-dropdown" onclick="toggleMenu()">▼</button>
        <nav id="header-nav">
            <button id="log-in" class="nav-button" style="display: none;"><%= login %></button>
            <button id="create-subject" class="nav-button" style="display: none;"><%= create_subject %></button>
            <button id="find-subject" class="nav-button" style="display: none;"><%= find_subject %></button>
        </nav>
    </header>