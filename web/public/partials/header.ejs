<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page %> - Quizconst</title>
    <link rel="stylesheet" href="style/main.css">
</head>
<body>
    <header>
        <form method="get" id="language-form">
            <select name="language" id="language-selection" onchange="this.form.submit()">
                <option value="fi" <%= language === 'fi' ? 'selected' : '' %>>FI</option>
                <option value="en" <%= language === 'en' ? 'selected' : '' %>>EN</option>
            </select>
        </form>
        <h1 id="main-title">Quizconst</h1>
    </header>